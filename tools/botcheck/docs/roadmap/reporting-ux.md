# Roadmap — reporting, transparency & UX (G53–G58)

*(part of [roadmap index](README.md))*

Ratings key: see [README.md § How to read the ratings](README.md#how-to-read-the-ratings).

| # | Capability they provide | Who has it | Sev · Effort · Status | What they do that we don't → recommended move |
|---|---|---|---|---|
| G53 | Explicit on-page scope disclosure (what the verdict does/doesn't use) | deviceandbrowserinfo.com, incolumitas | medium · trivial · **Shipped** | State verbatim what verdict is/isn't based on (deviceandbrowserinfo: 'does NOT use IP reputation or behavior'; incolumitas: 'false positives are expected'). → **One-paragraph trust win: say plainly client fingerprint + headers + IP reputation, no behavior/ML; VPN/privacy users may score suspicious by design.** |
| G54 | Raw fingerprint / device-attributes dump for inspection | sannysoft, bot.incolumitas, Fingerprint.com, CreepJS | low · trivial · **Shipped** | Expose full raw collected fingerprint (navigator dump, screen, canvas hashes, full JSON payload) so user/engineer can diff masked browser against expectations. → **We show per-signal flagged/ok/not-collected breakdown, not raw values dump. Add collapsible 'raw fingerprint JSON' section — trivial, already have fused payload, materially helps debugging audience. JSON API already exposes server-side view; extend to include client payload.** → **Shipped 2026-07-18:** POST /check endpoint echoes fingerprint as `Report.ClientPayload` (server-observed fields stay `json:"-"`), collapsible "raw fingerprint" card in result.html renders it via `Signals.RawJSON`, JSON API exposes same `clientPayload`. |
| G55 | Educational per-signal explanations / learning zone | deviceandbrowserinfo.com, CreepJS, bot.incolumitas | low · low · **Shipped** | Pair each signal w/ technical write-up of why it fires + limitations (deviceandbrowserinfo's 'learning zone'; bot.incolumitas openly versioned w/ author caveats) — builds trust as reference. → **Our breakdown transparent but terse. Add short per-signal 'why this matters' tooltips/expanders + honest limitations note (e.g. CDP false-positives on real DevTools users). Cheap, exactly what makes these pages trusted references — strong fit for portfolio tool.** → **Shipped 2026-07-18, grown since:** every rule ID (all 66 implemented, +1 reserved — `system_color_headless`) has 1–2-sentence write-up in `ruleExplanations` ([`../../report.go`](../../report.go)) covering what fires it AND known false-positive caveat; each breakdown row gets collapsed "why" expander. White-box test (`report_internal_test.go`) fails build if any rule lands without one. **2026-07-19 update:** several explanations (`cdp_both`/`cdp_main_only`/`cdp_sw_only`/`webdriver_sw`/`chrome_runtime_tamper`) rewritten to state real audit findings instead of theoretical caveats — see [`../testing/findings-log.md`](../testing/findings-log.md). |
| G56 | Name the detected environment (browser/engine version, anti-detect browser) | Fingerprint.com, iphey.com | low · low · **Shipped** | State detected environment plainly ('Electron 42.5.1') as credibility flex; iphey can sometimes name which anti-detect browser in use. → **We detect embedded-runtime tokens (Electron/CEF/etc.) but don't prominently name+version environment back to user. Surface 'detected environment' line in report — cheap credibility using data already parsed.** → **Shipped 2026-07-18:** `Signals.Environment()` names browser + major version, OS, engine family ("Chrome 125 · macOS · Blink"), embedded runtimes w/ full version ("Electron 32.1.2 (embedded Chromium)"), shown as muted "Detected environment:" line in verdict card; "" when UA unparseable hides line. |
| G57 | Purpose-scoped report pages (verdict / behavior / network separated) | deviceandbrowserinfo.com, incolumitas, BrowserScan.net | low · medium · **Not built** | Split into distinct pages — fingerprint verdict vs behavioral test vs network/IP+header (+TLS) visualizer — each concern scannable in isolation. → **Largely deliberate choice (one page simpler); revisit only if signal table outgrows one screen.** |
| G58 | Bits-of-entropy / 'one in X' per-attribute reporting | AmIUnique.org, EFF Cover Your Tracks | low · ml-or-db · Deferred (documented) | Report each attribute's identifying power in Shannon-entropy bits + 'one in X browsers share this value' — ready-made explainability/weighting model. → **Meaningful only against population corpus we don't have (ties to crowd-rarity gap in [ip-reputation.md](ip-reputation.md), G40). Defer w/ that work. Entropy framing, however, good reference for weighting signals if we ever build corpus.** |
