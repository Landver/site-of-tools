# Roadmap — scoring model & cross-layer fusion (G48–G52)

*(part of the [roadmap index](README.md))*

Ratings key: see [README.md § How to read the ratings](README.md#how-to-read-the-ratings).
For how scoring actually works today, see [`../scoring-model.md`](../scoring-model.md).

| # | Capability they provide | Who has it | Sev · Effort · Status | What they do that we don't → recommended move |
|---|---|---|---|---|
| G48 | Cross-layer OS coherence (UA/OS vs TCP vs TLS vs GPU) | bot.incolumitas, DataDome, BrowserScan.net | medium · high-infra · **Deferred (dead end, confirmed 2026-07-21)** | Correlate claimed UA/OS against TCP/IP-inferred OS, TLS-implied OS, GPU/canvas-derived device class, so internally-consistent JS spoof still collapses when packet or handshake disagrees. This cross-check = whole point of their transport fingerprints. → **Depends entirely on TLS/TCP inputs in [network-layer.md](network-layer.md) (G26/G27/G29/G30), now a confirmed dead end, not a "not yet built" item to sequence after — Cloudflare proxied mode means any transport-layer read at our origin = Cloudflare's own edge-to-origin connection, not visitor's, so no real transport-implied OS to compare against. Closes with them. We already do JS-layer half (UA vs userAgentData.platform); that's as far as this rule goes.** |
| G49 | Per-signal confidence indicators | Fingerprint.com | low · low · **Not built** | Attach confidence level to individual signals (VPN, residential-proxy) and to overall identification, so consumers can weight uncertain signals. → **Our tiered weights already encode rough confidence (hard/consistency/soft). Light per-row confidence label layerable cheaply for richer reporting, but cosmetic. Low priority.** |
| G50 | Separate like-headless / headless / stealth ratings + chromium readout | CreepJS | low · low · **Reverted** | Report three independent percentages (likeHeadless / headless / stealth) plus chromium:true/false engine boolean, separating genuine engine quirk from active stealth patching. → **Presentation idea: derive a couple sub-scores from existing signals so 'real engine but patched' reads differently from 'headless build'.** → **Shipped 2026-07-18, reverted 2026-07-19:** `Report.TierScore(tier)` re-derived per-tier sub-score, but result page reused single tier-wide "consistency" score in all four consistency subgroup cards (network/UA/context/internals) instead of subgroup-scoped one — card could show e.g. "browser internals cross-checks — 50/100" while every check inside = "ok". Removed per-card score line entirely (all six breakdown cards) rather than build subgroup-scoped score; `TierScore` + tests deleted as dead code. |
| G51 | Time-staggered re-scoring as telemetry accumulates | bot.incolumitas | low · low · **Not built** | Recompute verdict at intervals (1.5/4/7/10/15s) so later passes use more interaction data + trim false positives. → **Only meaningful w/ behavioral telemetry (deferred — see [behavioral.md](behavioral.md)). Our single-shot fuse appropriate for stateless self-test. Skip unless behavioral scoring lands.** |
| G52 | ML risk model / trained classifier over signal vector | bot.incolumitas, Fingerprint.com, DataDome | low · ml-or-db · Deferred (documented) | Replace/augment hand-weighted rules w/ supervised classifier (Fingerprint: server-side ML bot verdict; DataDome: supervised + genetic + anomaly ensembles per-customer baseline). → **Deliberately deferred — our value prop = transparent, deterministic, no-ML scorer whose every deduction explainable. Keep pure scorer; note ML as intentional non-goal, not blind spot.** |
