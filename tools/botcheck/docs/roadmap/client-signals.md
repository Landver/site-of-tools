# Roadmap — client-side detection signals (G01–G25)

*(part of the [roadmap index](README.md))*

Sorted by severity then effort. Every row is one capability a competitor
provides that we don't fully match; final column states what they do and
recommended move for our stack. Ratings key: see
[README.md § How to read the ratings](README.md#how-to-read-the-ratings).

| # | Capability they provide | Who has it | Sev · Effort · Status | What they do that we don't → recommended move |
|---|---|---|---|---|
| G01 | Expand userAgentData high-entropy hints + platformVersion coherence | CreepJS, iphey.com, Fingerprint.com | medium · trivial · **Shipped** | Pull full getHighEntropyValues set (architecture, bitness, model, platformVersion, uaFullVersion, fullVersionList), cross-check vs UA. CreepJS caught UA claiming macOS 10_15_7 while userAgentData reported macOS 26.5.1 — frozen-Electron/spoof tell. → **Shipped** — implementation, what was trimmed and why: [checks/ua_chrome_version_mismatch.md](../testing/checks/ua_chrome_version_mismatch.md). |
| G02 | navigator.productSub / oscpu / buildID / pdfViewerEnabled | iphey.com, AmIUnique.org, CreepJS | medium · trivial · **Shipped (productSub only; pdfViewerEnabled dropped as a user-setting false-fire, oscpu/buildID skipped)** | productSub classic engine tell (Chromium always '20030107', Gecko '20100101'); oscpu/buildID/pdfViewerEnabled add OS/engine consistency + headless tell (pdfViewerEnabled often false headless). → **Shipped** — what shipped, what was tried and dropped: [checks/productsub_mismatch.md](../testing/checks/productsub_mismatch.md). |
| G03 | Broaden cross-context (worker/iframe/SW) comparison beyond UA | deviceandbrowserinfo.com, bot.incolumitas, CreepJS | medium · low · **Shipped** | Recompute + diff more than UA across contexts — languages, hardwareConcurrency, platform, even WebGL renderer/fonts — between main thread, Web Worker, Service Worker, iframe. Caught Bright Data returning Linux in worker while top UA claimed Windows. → **Shipped**, five rules: [checks/context_ua_mismatch.md](../testing/checks/context_ua_mismatch.md), [context_language_mismatch](../testing/checks/context_language_mismatch.md), [context_cores_mismatch](../testing/checks/context_cores_mismatch.md), [context_platform_mismatch](../testing/checks/context_platform_mismatch.md), [context_webgl_mismatch](../testing/checks/context_webgl_mismatch.md). |
| G04 | Deep native-function tamper / lie detection | CreepJS, deviceandbrowserinfo.com, bot.incolumitas, BrowserScan.net, Pixelscan, Fingerprint.com | medium · low · **Shipped** | Go well beyond toString '[native code]' check: CreepJS's queryLies checks each API for illegal own-properties/descriptors (prototype/arguments/caller), traps whether call/new/apply/class-extends throw correct TypeError, detects `Function.prototype.toString` Proxy puppeteer-extra-stealth installs via error-stack frame inspection. bot.incolumitas targets stealth-plugin artefacts directly (puppeteerExtraStealthUsed, overrideTest). → **Shipped**, three rules — implementation, the false-positive fixed before deploy, and the 2026-07-19 evasion/fix each one had: [checks/tostring_proxy.md](../testing/checks/tostring_proxy.md), [native_descriptor_tamper](../testing/checks/native_descriptor_tamper.md), [native_callnew_tamper](../testing/checks/native_callnew_tamper.md). |
| G05 | Feature-detect true engine and compare to claimed UA _(we do a narrower version)_ | iphey.com, CreepJS | medium · low · **Shipped** | Feature-detect actual rendering engine/version (Chromium via webkitResolveLocalFileSystemURL + BatteryManager + vendor; Gecko via buildID + onmozfullscreenchange; WebKit via ApplePayError), cross-check vs claimed UA — catches spoofed UAs + anti-detect browsers a string parse misses. → **Shipped** — narrower than competitors (feature probes only, no version): [checks/engine_ua_mismatch.md](../testing/checks/engine_ua_mismatch.md). |
| G06 | HTTP header value/presence consistency vs claimed browser | AmIUnique.org, incolumitas, DataDome | medium · low · **Shipped** | Inspect presence + VALUES of Accept, Accept-Encoding, Upgrade-Insecure-Requests, Connection, Cache-Control etc, check coherence w/ claimed browser (beyond just header order). → **Shipped**, three rules: [checks/accept_encoding_missing.md](../testing/checks/accept_encoding_missing.md) (also covers why `Upgrade-Insecure-Requests` was captured but left unused), [accept_language_missing](../testing/checks/accept_language_missing.md), [accept_nav_mismatch](../testing/checks/accept_nav_mismatch.md). |
| G07 | WebGL vendor/renderer/feature internal inconsistency | deviceandbrowserinfo.com, incolumitas | medium · low · **Shipped** | Check UNMASKED_VENDOR vs UNMASKED_RENDERER + GPU parameter/feature set for internal self-contradiction (distinct from our software-renderer test + GPU-vs-OS coherence). → **Shipped**, including the 2026-07-19 collector bug that silently neutered it: [checks/webgl_vendor_mismatch.md](../testing/checks/webgl_vendor_mismatch.md). |
| G08 | WebGL/GPU identity vs claimed OS/UA coherence | Pixelscan, CreepJS, DataDome, iphey.com, deviceandbrowserinfo.com | medium · low · **Shipped** | Cross-check unmasked GPU vendor/renderer vs claimed platform (e.g. Chrome-on-Windows UA whose canvas/WebGL renderer matches Apple/Metal GPU flagged). CreepJS also diffs worker-scope WebGL renderer vs main (hasBadWebGL). → **Shipped** (worker-vs-main half shipped with G03 as `context_webgl_mismatch`; same 2026-07-19 collector-bug history as G07): [checks/gpu_os_mismatch.md](../testing/checks/gpu_os_mismatch.md). |
| G09 | WebRTC local/public IP leak (STUN, mDNS candidates) | bot.incolumitas, CreepJS, BrowserScan.net, Pixelscan, iphey.com, whoer.net | medium · medium · **Shipped** | Use STUN request (e.g. stun.l.google.com:19302) to enumerate ICE candidates, extract real local (RFC1918) + public IP behind VPN/proxy, flag WebRTC-real-IP vs egress-IP mismatch. → **Shipped**: [checks/webrtc_ip_mismatch.md](../testing/checks/webrtc_ip_mismatch.md). |
| G10 | Cheap headless render tells (battery, broken-image, hairline, system-color) | sannysoft, CreepJS, bot.incolumitas, iphey.com | low · trivial · **Shipped (broken-image only)** | Small client render/behavior probes: getBattery presence/behavior (CHR_BATTERY), broken-image 0x0 natural dimensions, Modernizr 0.5px hairline offsetHeight quirk, CreepJS's hasKnownBgColor (render CSS system color ActiveText, expect rgb(255,0,0) in headless). → **Shipped (broken-image only)** — why the system-color and battery/hairline probes were dropped: [checks/image_broken.md](../testing/checks/image_broken.md). |
| G11 | iframe proxy/override detection + webdriver-in-iframe | CreepJS, deviceandbrowserinfo.com | low · trivial · **Shipped** | CreepJS's hasIframeProxy builds srcdoc iframe, inspects contentWindow for proxied window; deviceandbrowserinfo re-checks navigator.webdriver inside iframe (hasWebdriverInFrameTrue) to catch evasions only patching main frame. → **Shipped**, two rules: [checks/iframe_webdriver.md](../testing/checks/iframe_webdriver.md), [iframe_proxy](../testing/checks/iframe_proxy.md). |
| G12 | Additional fingerprint-entropy surfaces (audio, WebGPU, DOMRect, media devices, speech, touch) | CreepJS, bot.incolumitas, Fingerprint.com, BrowserScan.net, iphey.com, AmIUnique.org, EFF Cover Your Tracks | low · low · **Shipped (touch only; rest deferred)** | Collect AudioContext fingerprint, WebGPU capabilities, DOMRect/SVGRect/clientRects geometry, media-device enumeration, speech-synthesis voices, touch/maxTouchPoints as fingerprint entropy + (in CreepJS) cross-context consistency inputs. → **Shipped (touch only)**: [checks/mobile_no_touch.md](../testing/checks/mobile_no_touch.md). Audio + remaining entropy surfaces stay deferred (value only w/ crowd/rarity DB). |
| G13 | Broaden automation-framework signature battery | BrowserScan.net, sannysoft, deviceandbrowserinfo.com, bot.incolumitas | low · low · **Shipped** | Cover more frameworks by name: Sequentum (window.external), Awesomium, CEF/CefSharp, FMiner, Rhino, WebdriverIO, SlimerJS, plus puppeteer-extra-stealth + puppeteer evaluation-script artefacts. BrowserScan reports 15+ individually. → **Shipped**, including why CefSharp/Awesomium/CEF are deliberately excluded (that's `embedded_runtime`'s job) and the 2026-07-19 finding on Playwright binding hooks: [checks/framework_globals.md](../testing/checks/framework_globals.md). |
| G14 | CDP / navigator recheck in a Service Worker context | bot.incolumitas, CreepJS | low · low · **Shipped, downgraded 2026-07-19** | Run CDP + navigator consistency checks inside Service Worker (not just Web Worker); bot.incolumitas's inconsistentServiceWorkerNavigatorPropery flagged Electron/CDP browser whose main-thread webdriver read clean. → **Shipped, downgraded 2026-07-19** — full audit: [checks/webdriver_sw.md](../testing/checks/webdriver_sw.md), [checks/cdp_sw_only.md](../testing/checks/cdp_sw_only.md). |
| G15 | CSS media-query / computed-style / display-capability fingerprint | CreepJS, iphey.com | low · low · **Shipped (media-query probes only)** | Probe prefers-color-scheme, color gamut, HDR/HDCP, forced/inverted colors, reduced-motion/transparency, devicePixelRatio, CSS system colors + computed styles. → **Cheap client probes; add a few (devicePixelRatio vs screen, forced-colors) as consistency/entropy surfaces.** → **Shipped (media-query probes only)** — what's entropy-only vs scored, and the DPR/system-color probes deliberately not built: [checks/matchmedia_missing.md](../testing/checks/matchmedia_missing.md). |
| G16 | DevTools-open detection (debugger timing + window-size delta) | iphey.com, Fingerprint.com | low · low · **Not built — skip (re-evaluated 2026-07-19)** | iphey times a `debugger` statement in a worker, checks outerWidth - innerWidth > 160px; Fingerprint exposes a 'Developer Tools' Smart Signal. Weaker automation proxy than CDP but a distinct signal. → **Originally skipped as "redundant with the CDP trap"; assumption was wrong (see G14/CDP-trap finding above), why this got re-opened. Re-evaluated properly this time: it detects a human with DevTools open, not automation — Puppeteer/Playwright/Selenium don't open a visible DevTools panel by default, confirmed against all 5 frameworks in the [2026-07-19 audit](../testing/findings/2026-07-19-multi-framework-matrix-results.md), none of which would have tripped it. Building it would mean scoring a curious, technically-literate human (anyone who opens DevTools to poke at a page) as more suspicious, with no corresponding automation coverage gained — the opposite of what this tool is for. Re-closed as skip, this time for the right reason: not "redundant," but "measures the wrong population."** |
| G17 | Full window-global enumeration + navigator prototype descriptor walk | sannysoft, CreepJS, iphey.com, Pixelscan | low · low · **Shipped** | Enumerate all window globals, walk navigator's prototype property descriptors to surface injected automation globals + getter overrides (faked webdriver/plugins), rather than only reading spoofable values. → **Shipped**, and folded into `framework_globals`' sweep — see [checks/navigator_proto_tamper.md](../testing/checks/navigator_proto_tamper.md) and [checks/framework_globals.md](../testing/checks/framework_globals.md). |
| G18 | Impossible-geometry / recursion-overflow headless tells | bot.incolumitas, sannysoft | low · low · **Not built** | overflowTest / resOverflow deliberately trigger a recursion stack overflow, read error message/signature; plus impossible screen/window geometry checks used as headless indicators. → **Mostly legacy PhantomJS-era tells, limited modern value. Skip the overflow probe; already cover several geometry anomalies (availScreen>physical, outer<inner, 800x600) in our soft cluster.** |
| G19 | Incognito / private-mode detection | Fingerprint.com, BrowserScan.net, Pixelscan | low · low · **Not built** | Detect private/incognito mode via storage-quota + filesystem-API heuristics, surface as a signal. → **Low value as a bot tell (humans use incognito constantly), detection tricks are brittle/version-specific. Skip, or add only as an informational (non-scoring) line if we want parity in the report.** |
| G20 | Privacy/anti-detect tool resistance detection | CreepJS | low · low · **Not built** | Detect Tor Browser, Firefox RFP, Brave, ungoogled-chromium, and extensions (uBlock/NoScript/CanvasBlocker/Chameleon/ScriptSafe) and measure how well the mask holds. → **Niche (serves the anti-detect audience, not bot detection). Skip, or add a small informational readout later. Not a scoring signal for us.** |
| G21 | Storage/quota, Network Information, MediaCapabilities/EME, GPC, full Permissions enumeration | iphey.com, CreepJS, AmIUnique.org, incolumitas | low · low · **Shipped (as entropy + one coherence rule)** | Probe localStorage/indexedDB presence + quota, Network Information (rtt/downlink/effectiveType — incolumitas' connectionRTT), MediaCapabilities/EME-DRM, Global Privacy Control, enumerate all Permissions states. → **Cheap additional entropy/consistency surfaces; connectionRTT vs IP geo is a genuinely new cross-check.** → **Shipped (as entropy + one rule)** — what's scored vs entropy-only, and everything deliberately skipped: [checks/netinfo_incoherent.md](../testing/checks/netinfo_incoherent.md). |
| G22 | chrome.runtime integrity + late-injection index checks | CreepJS | low · low · **Shipped** | hasBadChromeRuntime instantiates chrome.runtime.sendMessage/connect, inspects for missing prototype / wrong error constructor to unmask a faked chrome object; hasHighChromeIndex flags 'chrome' appearing among the last ~50 window keys (stealth patches inject it late). → **Shipped**, two rules — including the most heavily investigated open item in the whole 2026-07-19 audit: [checks/chrome_runtime_tamper.md](../testing/checks/chrome_runtime_tamper.md), [checks/chrome_late_injection.md](../testing/checks/chrome_late_injection.md). |
| G23 | JS-engine fingerprint (Math results, window/HTMLElement key sets, error-stack) | CreepJS | low · medium · **Shipped (error-stack engine only)** | Fingerprint the JS engine/version from Math function results, Error-stack engine signatures, and window/HTMLElement key enumeration; flag out-of-range feature versions. → **Shipped (error-stack only)**: [checks/jsengine_ua_mismatch.md](../testing/checks/jsengine_ua_mismatch.md). |
| G24 | Virtual-machine / emulator detection | Fingerprint.com | low · medium · **Not built** | Flag VM signatures + Android emulators (partly server-correlated), treat a VM as bot=bad. → **Browser-observable VM tells overlap heavily with our software-renderer (swiftshader/llvmpipe) check, which we already have. Little incremental value client-only; skip dedicated VM detection.** |
| G25 | Mobile SDK native signals (root/jailbreak, Frida, emulator, OS attestation) | Fingerprint.com, DataDome | low · high-infra · **Not built** | Native mobile SDKs collect Frida instrumentation, root/jailbreak, emulator/simulator, cloned-app, MITM, tampered-request, and platform attestation (Play Integrity/SafetyNet, App Attest/DeviceCheck) — a signal class with no browser equivalent. → **Not applicable — botcheck is a web page with no mobile app/SDK. Note as an out-of-scope capability class, not a gap to close.** |
