# Roadmap — client-side detection signals (G01–G25)

*(part of the [roadmap index](README.md))*

Sorted by severity then effort. Every row is one capability a competitor
provides that we don't fully match; final column states what they do and
recommended move for our stack. Ratings key: see
[README.md § How to read the ratings](README.md#how-to-read-the-ratings).

| # | Capability they provide | Who has it | Sev · Effort · Status | What they do that we don't → recommended move |
|---|---|---|---|---|
| G01 | Expand userAgentData high-entropy hints + platformVersion coherence | CreepJS, iphey.com, Fingerprint.com | medium · trivial · **Shipped** | Pull full getHighEntropyValues set (architecture, bitness, model, platformVersion, uaFullVersion, fullVersionList), cross-check vs UA. CreepJS caught UA claiming macOS 10_15_7 while userAgentData reported macOS 26.5.1 — frozen-Electron/spoof tell. → **Shipped 2026-07-17, trimmed same day: `navigator.userAgentData.platform` read directly, `fullVersionList` requested via `getHighEntropyValues(['fullVersionList'])`; `ua_chrome_version_mismatch` compares UA's Chrome/NNN major against `Chromium` (falling back to `Google Chrome`) `fullVersionList` entry. `platformVersion`/`uaFullVersion`/`architecture`/`bitness`/`model` requested briefly then dropped as unused (YAGNI) — no rule compares UA-embedded OS version vs `userAgentData.platformVersion`, since `platformVersion` not collected.** |
| G02 | navigator.productSub / oscpu / buildID / pdfViewerEnabled | iphey.com, AmIUnique.org, CreepJS | medium · trivial · **Shipped (productSub only; pdfViewerEnabled dropped as a user-setting false-fire, oscpu/buildID skipped)** | productSub classic engine tell (Chromium always '20030107', Gecko '20100101'); oscpu/buildID/pdfViewerEnabled add OS/engine consistency + headless tell (pdfViewerEnabled often false headless). → **Shipped productSub as engine-aware consistency rule. pdfViewerEnabled tried then dropped: false on ordinary desktop Chrome w/ "Download PDFs" set or AlwaysOpenPdfExternally policy — flags user preference, not headless.** |
| G03 | Broaden cross-context (worker/iframe/SW) comparison beyond UA | deviceandbrowserinfo.com, bot.incolumitas, CreepJS | medium · low · **Shipped** | Recompute + diff more than UA across contexts — languages, hardwareConcurrency, platform, even WebGL renderer/fonts — between main thread, Web Worker, Service Worker, iframe. Caught Bright Data returning Linux in worker while top UA claimed Windows. → **Shipped 2026-07-17:** worker + iframe also report languages/cores/`userAgentData.platform`, worker tries OffscreenCanvas WebGL renderer; Service-Worker context added (`/botcheck-sw.js`, served by app, no fetch handler, unregistered after probe). Four new consistency rules (`context_language/cores/platform/webgl_mismatch`) plus SW side of `context_ua_mismatch`, each firing only when both sides confidently report. |
| G04 | Deep native-function tamper / lie detection | CreepJS, deviceandbrowserinfo.com, bot.incolumitas, BrowserScan.net, Pixelscan, Fingerprint.com | medium · low · **Shipped** | Go well beyond toString '[native code]' check: CreepJS's queryLies checks each API for illegal own-properties/descriptors (prototype/arguments/caller), traps whether call/new/apply/class-extends throw correct TypeError, detects `Function.prototype.toString` Proxy puppeteer-extra-stealth installs via error-stack frame inspection. bot.incolumitas targets stealth-plugin artefacts directly (puppeteerExtraStealthUsed, overrideTest). → **Shipped 2026-07-17:** three new probes — descriptor/own-property sanity (w/ per-spec enumerability: WebIDL ops `enumerable: true`, ECMA-262 built-ins not — asserting blanket-false false-fired every real browser), call/new `TypeError` traps, toString-Proxy probe (shape differential vs control native + error-stack apply-frame inspection). Rules: `tostring_proxy` (hard), `native_descriptor_tamper` + `native_callnew_tamper` (consistency, since legit privacy extensions can patch DOM APIs). Payload versioned (`v: 2`) so stale cached collector skips these instead of reading as tampered. **2026-07-19 finding:** all three evaded by current `puppeteer-extra-plugin-stealth` — see [the multi-framework matrix results](../testing/findings/2026-07-19-multi-framework-matrix-results.md). |
| G05 | Feature-detect true engine and compare to claimed UA _(we do a narrower version)_ | iphey.com, CreepJS | medium · low · **Shipped** | Feature-detect actual rendering engine/version (Chromium via webkitResolveLocalFileSystemURL + BatteryManager + vendor; Gecko via buildID + onmozfullscreenchange; WebKit via ApplePayError), cross-check vs claimed UA — catches spoofed UAs + anti-detect browsers a string parse misses. → **Shipped 2026-07-17: `engineFamily()` feature-detects real rendering engine (`-moz-appearance` ⇒ Gecko, `GestureEvent` ⇒ WebKit, `-webkit-app-region`/`webkitRequestFileSystem` ⇒ Blink, independent of UA string); `engine_ua_mismatch` (consistency, weight 30) fires when it disagrees w/ engine `engineFromUA` infers from claimed User-Agent.** |
| G06 | HTTP header value/presence consistency vs claimed browser | AmIUnique.org, incolumitas, DataDome | medium · low · **Shipped** | Inspect presence + VALUES of Accept, Accept-Encoding, Upgrade-Insecure-Requests, Connection, Cache-Control etc, check coherence w/ claimed browser (beyond just header order). → **Shipped 2026-07-17:** three soft rules (`accept_encoding_missing`, `accept_language_missing`, `accept_nav_mismatch`) keyed on `looksLikeBrowser(UA)` — soft precisely because proxies can strip/rewrite headers (the `sec_fetch_missing` caveat). Upgrade-Insecure-Requests captured but deliberately unused: Safari never sends it, so any rule requiring it would false-positive real Safari. |
| G07 | WebGL vendor/renderer/feature internal inconsistency | deviceandbrowserinfo.com, incolumitas | medium · low · **Shipped** | Check UNMASKED_VENDOR vs UNMASKED_RENDERER + GPU parameter/feature set for internal self-contradiction (distinct from our software-renderer test + GPU-vs-OS coherence). → **Shipped 2026-07-17:** collector reports vendor string too; `webgl_vendor_mismatch` fires when vendor + renderer parse to different confident families (e.g. vendor Apple vs renderer NVIDIA). Verified against real reporting styles (ANGLE shim pairs, Safari's generalised "Apple Inc."/"Apple GPU", Firefox driver strings) so no real browser contradicts itself. **2026-07-19 note:** rule silently never fired for anyone (bot or human) until `webglGPU()` collector bug — undefined variable — fixed; see [the `webglGPU()` bug finding](../testing/findings/2026-07-19-webglgpu-bug-fixed.md). |
| G08 | WebGL/GPU identity vs claimed OS/UA coherence | Pixelscan, CreepJS, DataDome, iphey.com, deviceandbrowserinfo.com | medium · low · **Shipped** | Cross-check unmasked GPU vendor/renderer vs claimed platform (e.g. Chrome-on-Windows UA whose canvas/WebGL renderer matches Apple/Metal GPU flagged). CreepJS also diffs worker-scope WebGL renderer vs main (hasBadWebGL). → **Shipped 2026-07-17:** `gpu_os_mismatch` fires only on enumerated impossible pairs (Apple GPU on Windows/Linux/Android, desktop NVIDIA/AMD on iOS/Android, Adreno/Mali on macOS/iOS) — deliberately silent on AMD+macOS (Intel Macs), Adreno+Windows (Snapdragon ARM), Intel+Android, unknown GPUs. Worker-vs-main WebGL diff half shipped w/ G03 (`context_webgl_mismatch`). Same 2026-07-19 collector-bug note as G07 applies (fixed). |
| G09 | WebRTC local/public IP leak (STUN, mDNS candidates) | bot.incolumitas, CreepJS, BrowserScan.net, Pixelscan, iphey.com, whoer.net | medium · medium · **Shipped** | Use STUN request (e.g. stun.l.google.com:19302) to enumerate ICE candidates, extract real local (RFC1918) + public IP behind VPN/proxy, flag WebRTC-real-IP vs egress-IP mismatch. → **Shipped 2026-07-18:** collector harvests candidate IPs over public STUN server (~1.5 s, mDNS `.local` names skipped); `webrtc_ip_mismatch` (consistency) fires only when a PUBLIC candidate differs from server-observed egress IP — private/loopback/link-local/ULA/CGNAT candidates excluded (normal NAT, never a tell), same address family only (dual-stack stays silent), empty candidates/egress ⇒ skip. |
| G10 | Cheap headless render tells (battery, broken-image, hairline, system-color) | sannysoft, CreepJS, bot.incolumitas, iphey.com | low · trivial · **Shipped (broken-image only)** | Small client render/behavior probes: getBattery presence/behavior (CHR_BATTERY), broken-image 0x0 natural dimensions, Modernizr 0.5px hairline offsetHeight quirk, CreepJS's hasKnownBgColor (render CSS system color ActiveText, expect rgb(255,0,0) in headless). → **Shipped 2026-07-18 (broken-image only):** `image_broken` (soft) — a 1×1 data-URI image that must load in any real browser; naturalWidth == 0 or an error event ⇒ stripped/headless. hasKnownBgColor built then **dropped before shipping**: ground-truthed in a real, headed Chrome 150 on macOS, where ActiveText computes to exactly rgb(255,0,0) — "headless default" now every real Chrome, tell dated. Battery/hairline skipped as dated. |
| G11 | iframe proxy/override detection + webdriver-in-iframe | CreepJS, deviceandbrowserinfo.com | low · trivial · **Shipped** | CreepJS's hasIframeProxy builds srcdoc iframe, inspects contentWindow for proxied window; deviceandbrowserinfo re-checks navigator.webdriver inside iframe (hasWebdriverInFrameTrue) to catch evasions only patching main frame. → **Shipped 2026-07-18:** `iframe_webdriver` (hard — iframe's fresh realm has own Navigator.prototype, leaks what patched top frame hides) and `iframe_proxy` (consistency — stealth contentWindow getter verifiably throws when fresh srcdoc frame's window read, CreepJS's exact hasIframeProxy mechanics). |
| G12 | Additional fingerprint-entropy surfaces (audio, WebGPU, DOMRect, media devices, speech, touch) | CreepJS, bot.incolumitas, Fingerprint.com, BrowserScan.net, iphey.com, AmIUnique.org, EFF Cover Your Tracks | low · low · **Shipped (touch only; rest deferred)** | Collect AudioContext fingerprint, WebGPU capabilities, DOMRect/SVGRect/clientRects geometry, media-device enumeration, speech-synthesis voices, touch/maxTouchPoints as fingerprint entropy + (in CreepJS) cross-context consistency inputs. → **Shipped 2026-07-18 (touch only):** `mobile_no_touch` (consistency, v3-gated) — Android/iOS UA reporting maxTouchPoints == 0, which no real phone does. Reverse direction (desktop UA + touch) deliberately not a rule: touch-screen Windows laptops would false-fire constantly. Audio + remaining entropy surfaces stay deferred (value only w/ crowd/rarity DB). |
| G13 | Broaden automation-framework signature battery | BrowserScan.net, sannysoft, deviceandbrowserinfo.com, bot.incolumitas | low · low · **Shipped** | Cover more frameworks by name: Sequentum (window.external), Awesomium, CEF/CefSharp, FMiner, Rhino, WebdriverIO, SlimerJS, plus puppeteer-extra-stealth + puppeteer evaluation-script artefacts. BrowserScan reports 15+ individually. → **Shipped 2026-07-18:** WINDOW_MARKERS extended w/ Playwright binding hooks (`__pwInitScripts` / `__playwright__binding__`), wider Selenium/Watir canon (intoli/fp-scanner lineage sannysoft runs), Sequentum via `window.external`. CefSharp/Awesomium/CEF deliberately excluded from hard-60 list — legit desktop apps embed those runtimes, UA-side `embedded_runtime` consistency rule already covers that class. **2026-07-19 finding:** Playwright binding-hook markers don't fire against a stock `playwright` launch w/ no bindings exposed — see [the multi-framework matrix results](../testing/findings/2026-07-19-multi-framework-matrix-results.md). |
| G14 | CDP / navigator recheck in a Service Worker context | bot.incolumitas, CreepJS | low · low · **Shipped, downgraded 2026-07-19** | Run CDP + navigator consistency checks inside Service Worker (not just Web Worker); bot.incolumitas's inconsistentServiceWorkerNavigatorPropery flagged Electron/CDP browser whose main-thread webdriver read clean. → **Shipped 2026-07-18:** `/botcheck-sw.js` also reports `navigator.webdriver`, runs same CDP Error.stack trap the worker probe uses. Rules: `webdriver_sw` (hard) and `cdp_sw_only` (soft as of 2026-07-19, was consistency). **2026-07-19 finding:** confirmed across 3 real automation frameworks `webdriver_sw` never reads true even when webdriver true elsewhere in same session, CDP trap never fires at all — see the [`webdriver_sw` finding](../testing/findings/2026-07-19-webdriver-sw-confirmed-across-frameworks.md) and the [CDP-trap finding](../testing/findings/2026-07-19-cdp-trap-family-confirmed-dead.md) for full audit. |
| G15 | CSS media-query / computed-style / display-capability fingerprint | CreepJS, iphey.com | low · low · **Shipped (media-query probes only)** | Probe prefers-color-scheme, color gamut, HDR/HDCP, forced/inverted colors, reduced-motion/transparency, devicePixelRatio, CSS system colors + computed styles. → **Cheap client probes; add a few (devicePixelRatio vs screen, forced-colors) as consistency/entropy surfaces.** → **Shipped 2026-07-18 (wave-2 probes batch):** collector payload now `v: 4` w/ one additive `env` section — devicePixelRatio, prefers-color-scheme, forced-colors, reduced-motion, dynamic-range/gamut (all fail-to-absent, all entropy-only: user preferences + hardware capabilities never scored) — plus one rule: `matchmedia_missing` (soft, v4-gated): browser UA w/o `window.matchMedia` is a stripped JS environment. DPR-vs-screen consistency rule was **deliberately not built**: zoom legitimately changes DPR + inner-window sizes while `screen.*` stays zoom-invariant in Chrome, so zoomed-out real windows false-fire it; CSS system colors stay dropped (real Chrome computes ActiveText to old headless value). |
| G16 | DevTools-open detection (debugger timing + window-size delta) | iphey.com, Fingerprint.com | low · low · **Not built — skip (re-evaluated 2026-07-19)** | iphey times a `debugger` statement in a worker, checks outerWidth - innerWidth > 160px; Fingerprint exposes a 'Developer Tools' Smart Signal. Weaker automation proxy than CDP but a distinct signal. → **Originally skipped as "redundant with the CDP trap"; assumption was wrong (see G14/CDP-trap finding above), why this got re-opened. Re-evaluated properly this time: it detects a human with DevTools open, not automation — Puppeteer/Playwright/Selenium don't open a visible DevTools panel by default, confirmed against all 5 frameworks in the [2026-07-19 audit](../testing/findings/2026-07-19-multi-framework-matrix-results.md), none of which would have tripped it. Building it would mean scoring a curious, technically-literate human (anyone who opens DevTools to poke at a page) as more suspicious, with no corresponding automation coverage gained — the opposite of what this tool is for. Re-closed as skip, this time for the right reason: not "redundant," but "measures the wrong population."** |
| G17 | Full window-global enumeration + navigator prototype descriptor walk | sannysoft, CreepJS, iphey.com, Pixelscan | low · low · **Shipped** | Enumerate all window globals, walk navigator's prototype property descriptors to surface injected automation globals + getter overrides (faked webdriver/plugins), rather than only reading spoofable values. → **Shipped 2026-07-18:** `navigator_proto_tamper` (consistency, v3-gated) — per WebIDL, `webdriver`/`plugins`/`languages` must be native, getter-only, enumerable+configurable accessors living on Navigator.prototype, never own data properties on the instance; only confident anomalies fire, probe failure ⇒ pass. Suspect-name own-property sweep now covers `window` as well as `document`, folded into existing `framework_globals`. **2026-07-19 finding:** evaded by current `puppeteer-extra-plugin-stealth` — see [the multi-framework matrix results](../testing/findings/2026-07-19-multi-framework-matrix-results.md). |
| G18 | Impossible-geometry / recursion-overflow headless tells | bot.incolumitas, sannysoft | low · low · **Not built** | overflowTest / resOverflow deliberately trigger a recursion stack overflow, read error message/signature; plus impossible screen/window geometry checks used as headless indicators. → **Mostly legacy PhantomJS-era tells, limited modern value. Skip the overflow probe; already cover several geometry anomalies (availScreen>physical, outer<inner, 800x600) in our soft cluster.** |
| G19 | Incognito / private-mode detection | Fingerprint.com, BrowserScan.net, Pixelscan | low · low · **Not built** | Detect private/incognito mode via storage-quota + filesystem-API heuristics, surface as a signal. → **Low value as a bot tell (humans use incognito constantly), detection tricks are brittle/version-specific. Skip, or add only as an informational (non-scoring) line if we want parity in the report.** |
| G20 | Privacy/anti-detect tool resistance detection | CreepJS | low · low · **Not built** | Detect Tor Browser, Firefox RFP, Brave, ungoogled-chromium, and extensions (uBlock/NoScript/CanvasBlocker/Chameleon/ScriptSafe) and measure how well the mask holds. → **Niche (serves the anti-detect audience, not bot detection). Skip, or add a small informational readout later. Not a scoring signal for us.** |
| G21 | Storage/quota, Network Information, MediaCapabilities/EME, GPC, full Permissions enumeration | iphey.com, CreepJS, AmIUnique.org, incolumitas | low · low · **Shipped (as entropy + one coherence rule)** | Probe localStorage/indexedDB presence + quota, Network Information (rtt/downlink/effectiveType — incolumitas' connectionRTT), MediaCapabilities/EME-DRM, Global Privacy Control, enumerate all Permissions states. → **Cheap additional entropy/consistency surfaces; connectionRTT vs IP geo is a genuinely new cross-check.** → **Shipped 2026-07-18 (wave-2 probes batch, in the v4 `env` section):** a `navigator.connection` sample (effectiveType/downlink/rtt/saveData — absent on Firefox/Safari, which is normal), rounded storage quota MB, GPC, a two-name Permissions sample, and EME ClearKey availability — all entropy for the raw dump, never scored (preferences and capabilities aren't bot tells). One rule: `netinfo_incoherent` (soft, v4-gated) — effectiveType claiming faster than its own reported rtt/downlink (browser derives the type from exactly those numbers; thresholds graced by the API's rounding). **Deliberately skipped:** incognito detection via quota (that's G19), the rtt-vs-IP-geo cross-check (client rtt measures same egress path the geo describes, so ordinary VPN users would false-fire), full Permissions enumeration (a two-name sample carries the entropy at no cost), and MediaCapabilities beyond ClearKey EME. |
| G22 | chrome.runtime integrity + late-injection index checks | CreepJS | low · low · **Shipped** | hasBadChromeRuntime instantiates chrome.runtime.sendMessage/connect, inspects for missing prototype / wrong error constructor to unmask a faked chrome object; hasHighChromeIndex flags 'chrome' appearing among the last ~50 window keys (stealth patches inject it late). → **Shipped 2026-07-18:** `chrome_runtime_tamper` (consistency, v3-gated — genuine runtime `sendMessage`/`connect` are native non-constructors: no own `prototype`, `new fn()` throws a `TypeError`; only confident contradictions fire) and `chrome_late_injection` (consistency — 'chrome' in the last ~50 of both the enumerable keys and the own property names). Both gated on a Chrome UA. **2026-07-19 finding:** both evaded by the current `puppeteer-extra-plugin-stealth`. A fix for `chrome_runtime_tamper` (flag `window.chrome` existing without `runtime` at all) was written and verified against stealth, then **reverted** — the official "Chrome for Testing" binary itself lacks `chrome.runtime` regardless of automation, and no genuine consumer-Chrome sample was available to rule out false-positiving real users. See [the multi-framework matrix results](../testing/findings/2026-07-19-multi-framework-matrix-results.md) — flagged there as the single most valuable open item. |
| G23 | JS-engine fingerprint (Math results, window/HTMLElement key sets, error-stack) | CreepJS | low · medium · **Shipped (error-stack engine only)** | Fingerprint the JS engine/version from Math function results, Error-stack engine signatures, and window/HTMLElement key enumeration; flag out-of-range feature versions. → **Shipped 2026-07-18 (error-stack only):** `jsengine_ua_mismatch` (consistency) — the Error-stack engine (V8 ` at ` frames; SpiderMonkey's proprietary `fileName`/`lineNumber` + `fn@url` frames; JSC otherwise) vs the engine the UA claims, mapped through `engineFromUA` (blink⇒v8, gecko⇒spidermonkey, webkit⇒jsc). Math-result and window/HTMLElement key-set probes stay deferred (need per-engine reference tables). |
| G24 | Virtual-machine / emulator detection | Fingerprint.com | low · medium · **Not built** | Flag VM signatures + Android emulators (partly server-correlated), treat a VM as bot=bad. → **Browser-observable VM tells overlap heavily with our software-renderer (swiftshader/llvmpipe) check, which we already have. Little incremental value client-only; skip dedicated VM detection.** |
| G25 | Mobile SDK native signals (root/jailbreak, Frida, emulator, OS attestation) | Fingerprint.com, DataDome | low · high-infra · **Not built** | Native mobile SDKs collect Frida instrumentation, root/jailbreak, emulator/simulator, cloned-app, MITM, tampered-request, and platform attestation (Play Integrity/SafetyNet, App Attest/DeviceCheck) — a signal class with no browser equivalent. → **Not applicable — botcheck is a web page with no mobile app/SDK. Note as an out-of-scope capability class, not a gap to close.** |
